<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<packaging>pom</packaging>
	
	<groupId>org.aktin</groupId>
	<artifactId>dwh-import</artifactId>
	<version>0.6-SNAPSHOT</version>
  
	<name>AKTIN : DWH Import</name>
	<description>AKTIN Software</description>

	<parent>
		<groupId>org.aktin</groupId>
		<artifactId>aktin</artifactId>
		<version>0.1-SNAPSHOT</version>
	</parent>
	
	<properties>
		<!-- specifies the package and source version to which the site refers to -->
		<latestReleaseVersion>0.6</latestReleaseVersion>
		<releaseRepoUrl>https://www.aktin.org/software/repo</releaseRepoUrl>
		<!-- encoding stuff -->
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
	</properties>

	<scm>
		<developerConnection>scm:git:git@gitlab.uni-oldenburg.de:AKTIN/dwh-import.git</developerConnection>
		<tag>HEAD</tag>
	</scm>

	<modules>
		<module>legacy-cda</module>
		<module>cda-validation</module>
		<module>cda-server</module>
		<module>cda-import</module>
		<module>cda-ontology</module>
		<module>demo-server</module>
		<module>demo-distribution</module>
		<module>dwh-j2ee</module>
		
		<!-- wegen fehlerhafter tests kurzzeitig entfernt
		<module>import-server</module>
		-->
	</modules>

		<!-- link to latest site online 
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>wagon-maven-plugin</artifactId>
				<version>1.0</version>
				<configuration>
					<serverId>www.aktin.org</serverId>
					<url>scp://www.aktin.org/httpdocs/software</url>
				</configuration>
				<executions>

					<execution>
						<id>ssh-exec</id>
						<phase>site-deploy</phase>
						<goals>
							<goal>sshexec</goal>
						</goals>
						<configuration>
							<displayCommandOutputs>true</displayCommandOutputs>
							<commands>
								<command>rm latest</command>
								<command>ln -s %{project.version} latest</command>
							</commands>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
		
	<profiles>
		<profile>
			<id>release-profile</id>
			<activation>
				<property>
					<name>performRelease</name>
					<value>true</value>
				</property>
			</activation>
			// does this work?? 
			<properties>
				<latestReleaseVersion>${project.version}</latestReleaseVersion>
			</properties>
		</profile>
	</profiles>
-->
</project>